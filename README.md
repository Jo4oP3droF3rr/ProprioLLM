# ProprioLLM
Criacao de um modelo LLM usando o Open Source da Open AI

imports usados 

pip install langgraph 
irm https://ollama.com/install.ps1 | iex
curl -fsSL https://ollama.com/install.sh
pip install pandas
caso queira deletar o LLM C:\Users\Seu_Usuario\.ollama\models
pip install langchain
pip install -U langchain-ollama <- foi usado no lugar do langchain.community.llms import Ollama, por que o mesmo foi substituido por ele